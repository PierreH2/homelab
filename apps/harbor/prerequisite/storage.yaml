# storage-harbor.yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: harbor-nas-pv
spec:
  capacity:
    storage: 25Gi
  accessModes: [ReadWriteOnce]
  hostPath:
    path: "/data/nas/public/registry"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: harbor-nas-pvc
  namespace: harbor
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 25Gi
  volumeName: harbor-nas-pv
